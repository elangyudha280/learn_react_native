

//! MATERI FORM DAN INPUT DI REACT NATIVE


//! APA ITU FORM DAN INPUT 
/*
    FORM/INPUT ADLAA SEBUAH COMPONENT UNTUK MENGAMBIL DATA USER
*///TODO: JADI USER BISA MEMASUKAN DATA


//! 2. MACAM" COMPONENT FORM DAN INPUT
// * ADA BANYAK LEBIH LENGKAP BISA CEK DI DOKUMENTASI
/*
        ! 2.1 TextInput
            * sebuah component untuk membuat input text
            * caranya import component <TextInput {...props}/>
            ! ada banyak props yg bisa digunakan bisa cek di dok
            ! ada beberapa yg biasa digunakan
            *1. style(object) = sebuah props yg digunakan utnk memberikan hiasan textnya
            *2. onChangeProps (callback(value))= sebuah props method untk melakukan aksi jika value input berubah
            *3.  value (string) = props untuk menentukan value yg akan tampil di inputnya 
            *4. placeholder (string) = props untuk menampilkan placeholder
            *5.  placeholderTextColor (color) = props untuk menentukan warna plaholder
            *6  secureTextEntry (boolean) = props untuk membuat input text menjadi password
            *7. keyboardType (enum) = props untk menentukan tipe keyboard apakah alphabet, number dan lain-lain
            *8 autoCorect = sebuah props untk mengkoreksi tulisan di input  

    
*/

//! 3. INPUT SWITCH
/*
    SWITCH ADALAH SEBUAH COMPONENT UNTUK MEMBUAT INPUT SWITCH ATAU PILIHAN 
*/

//! 3.1 CARA MENGGUNAKAN INPUT SWITCH
/*
    TODO: IMPORT DULU COMPONENNYA Switch

    *<Switch {...props} />

    * ada banyak props yg bisa digunakan bisa cek doknya
    * ada beberapa
    * 
    * 1. trackColor (object{true:value,false:value}) = sebuah props untuk mentukan warna background input switchnya
    * 2. thumbColor (color(rgb,hex,name)) = sebuah props untuk menentukan warna circle dari input switchnya 
*/

//! 4. apa itu KeyboardAvoidingView
/*
    KeyboardAvoidingView adalah sebuah component untk membuat pad input devuce tidak menghalangi form
*/